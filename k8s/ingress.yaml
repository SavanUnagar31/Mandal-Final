apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mandal-ingress
spec:
  rules:
  - host: mandal.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mandal-backend
            port:
              number: 80